"""Детерминированный планировщик календаря.

Ни сети, ни Django-моделей внутри логики, ни `now()`, ни случайности: стартовая
дата и все входные данные приходят аргументами. Та же дисциплина, что в
`curriculum/forecast.py` и `curriculum/planning/duration.py`, и по той же
причине — тест на переход летнего времени и на границу года обязан быть
воспроизводимым, а одинаковый вход обязан давать одинаковый календарь.

Слои:

    contracts.py   — типы обмена, без поведения
    pacing.py      — программа → PacingPlan (недельный ритм и части уроков)
    slots.py       — шаблон + занятость → конкретные свободные окна
    engine.py      — размещение частей по окнам
    reviews.py     — повторения через 2, 7 и 21 день
    materialize.py — черновик → строки LearningBlock (единственный слой с БД)
"""
