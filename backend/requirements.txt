asgiref==3.11.0
dj-database-url==3.0.1
Django==6.0
gunicorn==23.0.0
packaging==25.0
psycopg2-binary==2.9.11
# Векторный поиск: типы, поля Django и HNSW-индекс. Расширение `vector`
# ставится на сервере Postgres, эта библиотека — только клиентская часть.
pgvector==0.4.1
# S3-совместимое хранилище исходных PDF (Фаза 4a). Контейнер эфемерный,
# локальный диск переживает только один процесс.
boto3==1.35.99

# Очередь обработки учебников (Фаза 4b). Брокер — Redis; `redis` нужен именно
# как транспорт kombu, отдельным кэшем в проекте он не используется.
celery==5.4.0
redis==5.2.1

# Точный счётчик токенов (cl100k_base) для разбиения книги на фрагменты.
# ВАЖНО: файл кодировки скачивается при первом обращении, поэтому Dockerfile
# прогревает кэш на этапе сборки — иначе в проде граница фрагментов зависела бы
# от доступности CDN, а `content_hash` перестал бы быть воспроизводимым.
tiktoken==0.13.0
python-dotenv==1.2.1
sqlparse==0.5.5
whitenoise==6.11.0
django-cors-headers==4.3.1
djangorestframework==3.14.0
django-filter==25.2
opencv-python-headless==4.10.0.84
openai==2.41.0
requests==2.34.2
pypdfium2==4.30.0